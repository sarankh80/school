import 'package:flutter/material.dart';

class ReviewWedget extends StatelessWidget {
  @override
  Widget build(Object context) {
    return Row(
      mainAxisAlignment: MainAxisAlignment.start,
      children: [
        Container(
          child: Text("Saran Khan"),
        )
      ],
    );
  }
}
